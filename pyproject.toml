[project]
name = "rankings-app"
version = "0.1.0"
description = "Pairwise movie ranking with Glicko-2 ratings"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.32.0",
    "pandas>=2.0.0",
]
